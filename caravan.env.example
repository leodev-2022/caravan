# Caravan hub configuration (NON-secret). Copy to `caravan.env` and edit.
# Read by the installer and sourced by scripts. Secrets live separately in
# `conf/.env` (root-only on the hub) — never put tokens/passwords here.
DOMAIN=example.com
EMAIL=admin@example.com
HUB_IP=203.0.113.10
TZ=UTC
